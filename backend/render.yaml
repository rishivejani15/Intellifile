services:
  - type: web
    name: intellifile-signaling
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python signaling_server.py
    plan: free
    envVars:
      - key: PORT
        generateValue: false
