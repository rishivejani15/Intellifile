// Stub VersionTimeline component
import React from 'react';
export default function VersionTimeline({ filePath }) {
  return <div className="version-timeline">Version timeline for {filePath} (stub)</div>;
}
